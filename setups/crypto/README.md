## Crypto Extension

This setup writes a 256-byte buffer to a remote chiplet and reads it back through the crypto extension, which XOR-encrypts the outgoing data and decrypts it on the way back. Run with debug logging enabled to see the extension output.
